CONFIG_PRINTK=y
CONFIG_ASSERT=y
CONFIG_GPIO=y

CONFIG_SERIAL=y
CONFIG_UART_INTERRUPT_DRIVEN=y

# Enable the Ring Buffer subsystem for thread-to-thread comms
CONFIG_RING_BUFFER=y

# Enable the logging subsystem (better than printk for thread safety)
CONFIG_LOG=y
CONFIG_I2S=y